// inject root cascade layers
import "lib/styles/globals.css";

// export Tailwind preset (to be used in downstream Tailwind configurations)
export { default as hawkPreset } from "lib/plugin/hawk.preset";

export * from "ui";
